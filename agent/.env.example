# All three are auto-detected from your local machine — only set to override
# GITHUB_PAT=ghp_xxx           # Auto-detected from `gh auth token`
# GITHUB_OWNER=itayshmool      # Auto-detected from git remote
# GITHUB_REPO=from-dev-basics-to-claude-code  # Auto-detected from git remote

# API URL for posting triage results (server handles emails + stores in DB)
API_URL=https://terminal-trainer-api.onrender.com

# Set to true to log all decisions without executing any GitHub/git actions
DRY_RUN=false
